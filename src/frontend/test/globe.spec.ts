// // Playwright E2E test
// test("displays images on label click", async ({ page }) => {
//   await page.click(".globe-label");
//   await expect(page.locator(".image-carousel")).toBeVisible();
//   await page.click('button:has-text("Next")');
//   await expect(page.locator(".image-counter")).toContainText("2 / 3");
// });
