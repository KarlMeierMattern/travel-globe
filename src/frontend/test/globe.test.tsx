// Jest unit test
describe("Globe", () => {
  test("initializes globe with correct data", () => {
    // Test globe setup
  });

  test("handles label click correctly", () => {
    // Test label interaction
  });

  test("manages image carousel state", () => {
    // Test carousel logic
  });
});

// Used for component-level unit tests
// Can use JSX/TSX syntax
// Runs in Node environment
